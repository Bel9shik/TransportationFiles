allprojects {
    repositories {
        mavenCentral()
    }

    group = 'kardash.project.app'
    version = '1.0-SNAPSHOT'
}

buildscript {
    repositories {
        mavenCentral()
        gradlePluginPortal() // Добавляем Gradle Plugin Portal
    }
}